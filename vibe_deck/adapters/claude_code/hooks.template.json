{
  "_comment": "VibeDeck Claude Code Hook Configuration. Generated by: vibe-deck setup claude-code",
  "_note": "Generated by vibe-deck setup claude-code. The reporter path is resolved at generation time.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "SubagentStop": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "PreCompact": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "command": "python {{REPORTER_PATH}}"
      }
    ]
  }
}
