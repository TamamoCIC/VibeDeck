"""
VibeDeck Adapters — built-in and community.

Each adapter translates an external Agent's native output into
VibeDeck's display primitives (icon, color, animation, label, badge).

Built-in adapters ship with VibeDeck and cannot be removed.
Community adapters live under ~/.vibe-deck/adapters/<name>/.
"""
